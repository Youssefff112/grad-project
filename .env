# Backend Configuration
# Leave blank to auto-detect from Expo's dev-server host (recommended for LAN).
# Set to a full URL (e.g. https://abc.ngrok.io) for cross-network or production use.
EXPO_PUBLIC_BACKEND_URL=
EXPO_PUBLIC_API_PREFIX=/api/v1
EXPO_PUBLIC_ENV=development
